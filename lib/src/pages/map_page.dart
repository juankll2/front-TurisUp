import 'package:flutter/material.dart';
import 'package:turismup/src/pages/prueba.dart';

class MapPage extends StatelessWidget {
  const MapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mapas'),
      ),
      // body: ItemsList(),
    );
  }
}
